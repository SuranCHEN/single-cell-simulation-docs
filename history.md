---
title: 版本脉络
slug: history
---
# 版本脉络

模型、入口、目标和分母都曾变化。本页保留必要的版本区别，详细结果仍以对应输出为准。

| 阶段 | 主要设置 / 改变 | 与当前文档的关系 |
|---|---|---|
| 旧 small-n 与 clear-LD | 部分旧图使用 LD-block ABF 混合流程；旧噪声设置 cell=0.25、donor=0.8 | 历史诊断材料，不能作为当前 Original 的直接证据 |
| 独立的旧高深度探索 | 讨论/使用过 1,272、6,413、15,973 的细胞规模标签；与当前 target-cell 网格不同 | 不要写成“此前从未考察高深度”，也不直接映射真实队列 |
| 2026-09-07 formal | 12个固定2,000-SNP区域；n=50/100/200/500/750/1000/1250；m=10/50/200/1000 | 已锁定阈值的旧正式协议，详见[摘录](evidence/formal-20260907.json) |
| 2026-09-09–10 1 Mb 深度 | 12个1 Mb区域，六个开发/确认、六个 test；n=100/200；四档 m | 当前生成模型的主要来源；lead 为GWAS<5e−8、QTL<1e−5 |
| 2026-09-11 donor 扩展 | 六个历史区域；n=500/1000/1250；四档 m | 扩展 donor 轴；不构成新的独立位点测试 |
| 2026-09-12 对称入口 | n=100/200/500；四档 m；两侧 P<1e−5/1e−3/1e−2 | 当前 P<1e−3 入口的比较背景 |
| 2026-09-12–14 候选 / no-CS / 维度探索 | H0–H2、候选预算、原/扩展入口、维度参照评分 | 不同终点独立记录；未成功的扩展不被隐藏 |
| 2026-09-14 高召回 | n=100/200/500，m=10/1000；双侧1e−3；逐共享场景99%开发保护 | 当前主要评价协议 |
| 2026-09-15 新重复确认 | 固定n500×m1000及旧界值；五causal场景rep351–400，H0–H2 rep376–400 | 新重复证据；仍为相同六个位点 |
| 2026-09-17 文档整理 | 复用已有协议、代码及学习材料 | 本站没有开展新模拟或改变冻结参数 |

## 旧 formal 与当前设置的关键差异

旧 formal 的 Original 使用两侧 lead P<1e−5、min-block=1；1 Mb 深度主配置使用不对称入口且 min-block=5；当前高召回使用两侧1e−3、min-block=5。窗口也由固定 SNP 数改为固定物理宽度。不能把这些结果差异全归于“细胞数增加”或单个代码修复。

旧 formal 的主要问题之一是校准后的 no-CS 补充策略；当前主要问题是保护共享输入的预筛选。旧支持终点的“no-call 不计支持”和当前筛选的“no-call 转交”并不矛盾，它们回答不同问题。

## 需要保留的历史纠正

1. **方法身份。** 原始 LD 白化绝对余弦，与局部 ABF 最大 PP4/混合流程必须分名。
2. **donor 身份。** 旧批次曾出现保存的 donor 标签与实际矩阵行不一致；这不能扩写成所有 OLS 都随机错配。后来 bank 有身份、剂量及 LD 数值核对。
3. **distinct 的可分辨性。** 旧 double_distinct 部分窗口违反低 LD 约束，不能把这些输入当作干净的不共享机制对照。
4. **细胞数的作用。** 旧 cell 方差0.25、m=50时，均值细胞噪声只有0.005，远小于donor方差0.8；这一模型下增加深度的作用有限。
5. **阈值与分母。** 原始分数、最终支持、预筛选和no-CS预算是不同规则；NA、零分母和四舍五入不能改变判定。

这些纠正来自本地历史审计文件，而不是本次重新跑出的结果。历史证据索引见[来源页](sources.md)。

## 方法来源仍需限定

本地用户提供的 `WenxinJiang_jcsds_clike.pdf` 描述先选 block 再局部白化，与本地 Original 路线一致；历史 SBC 网页记录过全区域先白化的表述。没有拿到对方实际源码/commit 时，只能称为来源或版本差异，不能据此断言本地公式是 bug，或把差异当成性能原因。

## 新重复确认的实际执行链

9月15日批次已生成全部300个区域×重复 checkpoint，包含1,950条输入。原 `run.R` 在最后读取 gzip 汇总时因缺少 `R.utils` 停止；随后 `finish.R` 使用 R 自带 `gzfile` 读取已保存分数完成汇总，`verify.py` 复核通过。

实际链条是 `prepare.py → run.R 的模拟评分 → finish.R → verify.py`。这不是重算表型或重新校准阈值。判断最终状态应查 `COMPLETE.json` 与 `INDEPENDENT_CHECK.json`，不能仅凭早期错误日志判整个批次失败。
